# NGINX image using conf.d with per-host server configs generated at build time
FROM nginx:1.27-alpine

# Copy SSL certs into the image (generated by x-docker-build.sh into ./certs)
COPY certs /etc/nginx/certs

# Expose 443 (container internal)
EXPOSE 443

# Use default nginx entrypoint; configs will be mounted into /etc/nginx/conf.d by compose
